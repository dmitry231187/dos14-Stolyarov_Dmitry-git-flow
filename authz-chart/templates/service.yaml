apiVersion: v1
kind: Service
metadata:
  name: {{ include "authz-chart.fullname" . }}
  labels:
    {{- include "authz-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "authz-chart.selectorLabels" . | nindent 4 }}
